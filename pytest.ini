[pytest]
# Configure pytest with proper paths
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
addopts = -v --tb=short -p no:cacheprovider --capture=fd
# Disable problematic plugins and use fd (file descriptor) capturing to avoid I/O issues
